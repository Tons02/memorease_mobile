import { MD3LightTheme as DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#FAF7C1",
    secondary: "#1D6E29",
    background: "#FFFFFF",
    surface: "#FAF7C1",
    error: "#d32f2f",
    text: "#000000",
    onPrimary: "#000000",
    onSecondary: "#FFFFFF",
  },
};

export default theme;
